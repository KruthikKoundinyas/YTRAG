# RAG System v2 Requirements
# Core dependencies for production-ready RAG

# Embeddings and semantic search
sentence-transformers==5.3.0
faiss-cpu==1.13.2
numpy==1.26.4

# Flask (existing)
flask==3.1.3
requests==2.33.0

# Evaluation
rouge-score==0.1.2
nltk==3.9.4

# Note: Using numpy 1.26.4 for compatibility with faiss-cpu and sentence-transformers
# NumPy 2.x may have compatibility issues with PyTorch-based libraries
